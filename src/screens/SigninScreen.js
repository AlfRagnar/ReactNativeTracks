import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const SigninScreen = () => {
    return <Text style={styles.signin}>SigninScreen</Text>;
};

const styles = StyleSheet.create({
    signin: {
        fontSize: 48,
    },
});

export default SigninScreen;
